# Get this from Spotify Developer Dashboard at https://developer.spotify.com
# Create a new app, add http://localhost:3000/api/auth/callback/spotify as redirect URI
SPOTIFY_CLIENT_ID="your_spotify_client_id_here"

# Copy this from the same Spotify app you created above
SPOTIFY_CLIENT_SECRET="your_spotify_client_secret_here"

# Ask [your-name] privately for the shared database URL  
# This connects to our shared Render PostgreSQL database
DATABASE_URL="contact_partner_for_database_url"

# Generate a random string for JWT encryption (can use: openssl rand -base64 32)
NEXTAUTH_SECRET="your_random_secret_string_here"

# Local development URL - keep this exactly as shown
NEXTAUTH_URL="http://localhost:3000"